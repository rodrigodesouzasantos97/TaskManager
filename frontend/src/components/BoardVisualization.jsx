const BoardVisualization = ({ title }) => {
  return (
    <div className="Board-visualization">
      <h3>{title}</h3>
      <button>Ver quadro</button>
    </div>
  );
};

export default BoardVisualization;
